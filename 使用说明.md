# Link-Text Skill 使用说明

## 🎯 功能介绍

Link-Text Skill 是一个智能视频转录系统，能够将网络视频/音频转换为结构化的中文文本记录。

### 核心功能
- **多平台支持**：B站、YouTube、微博等主流平台
- **智能分段**：自动按2分钟分段处理长视频
- **质量优化**：根据硬件自动选择最佳模型，智能重录低质量段落
- **智能断句**：自动添加标点符号，提升阅读体验
- **主题识别**：自动分析内容主题并分类保存

## 🚀 快速使用

### 方法1：语音触发（最简单）
```
将视频转为文字 https://www.bilibili.com/video/BV1xxx
```

### 方法2：命令行执行
```bash
# 基础使用
python scripts/main_simplified.py <视频URL>

# 指定输出目录
python scripts/main_simplified.py <视频URL> D:/输出目录

# 使用智能监控版本（推荐）
python scripts/smart_transcribe.py <视频URL>
```

### 方法3：程序调用
```python
import subprocess

# 启动转录任务
subprocess.run(['python', 'scripts/main_simplified.py', url])
```

## 📂 输出说明

### 文件结构
```
translate/
├── 技术_20250731/
│   ├── audio/          # 分段音频文件（每2分钟一段）
│   ├── videos/         # 原始下载的视频文件
│   ├── output/         # 其他输出文件
│   ├── task_info.json  # 任务信息（元数据）
│   └── 转录_技术_20250731_1430.md  # 最终转录文件
├── 娱乐_20250731/
└── ...
```

### 转录文件格式
每个转录文件都包含：
- **YAML前置元数据**：视频信息、模型、主题等
- **目录**：按段落组织的快速导航
- **转录内容**：每段2分钟，带时间戳
- **自然断句**：自动添加标点符号

## ⚙️ 详细配置

### 模型选择策略
系统会自动检测硬件并选择最佳模型：

| 硬件配置 | 推荐模型 | 特点 |
|---------|---------|------|
| 8核+16GB内存 | large-v3 | 最高质量，速度较慢 |
| 4核+8GB内存 | medium | 平衡质量和速度 |
| 其他配置 | base | 基础质量，速度快 |

### 手动指定模型
```bash
# 设置环境变量
export TRANSCRIBE_MODEL=large-v3    # 最高质量
export TRANSCRIBE_MODEL=medium      # 平衡
export TRANSCRIBE_MODEL=base        # 基础质量

# 使用模型
python scripts/main_simplified.py <URL>
```

### 自定义输出目录
```bash
export TRANSCRIBE_OUTPUT_DIR=~/my_transcripts
```

## 🎨 主题分类

系统会自动识别内容主题，分为9大类：

1. **教程**：教学、学习、入门、指南
2. **技术**：编程、开发、软件、算法
3. **娱乐**：游戏、电影、音乐、动漫
4. **新闻**：时事、报道、资讯、热点
5. **科技**：AI、创新、机器学习、大数据
6. **教育**：课程、培训、考试
7. **商业**：投资、理财、创业、管理
8. **生活**：日常、美食、旅游、健康
9. **体育**：足球、篮球、奥运、运动

### 文件命名规则
- **目录名**：`{主题}_{日期}`，如 `技术_20250731`
- **文件名**：`转录_{主题}_{日期}_{时间}.md`，如 `转录_技术_20250731_1430.md`

## 🔧 质量控制

### 质量检查机制
系统会自动检查并改进转录质量：

1. **置信度检测**：低于阈值的段落自动重录
2. **文本长度检查**：过短的段落需要改进
3. **内容合理性**：检查填充词和标点自然度
4. **重试机制**：最多重录3次确保质量

### 参数优化
```python
# 大模型优化参数
beam_size=5,      # 增加搜索路径
best_of=5,        # 生成多个候选
temperature=0.0,  # 确定性结果
```

## 📊 智能监控

### 监控特点
- **静默启动**：前5分钟不检查，让程序正常运行
- **智能等待**：根据音频长度动态调整等待时间
- **结果导向**：只检查是否完成，不监控中间过程
- **资源节省**：减少90%的系统调用

### 使用监控器
```bash
# 使用智能转录工具（内置监控）
python scripts/smart_transcribe.py <URL>

# 或单独使用监控器
python scripts/main_simplified.py <URL>
python scripts/monitor_transcription.py 任务目录
```

## 🔄 工作流程

1. **系统检测**：自动检测CPU/内存性能
2. **下载媒体**：使用yt-dlp下载视频/音频
3. **音频转换**：转换为16kHz WAV格式
4. **分段处理**：按2分钟分段
5. **模型加载**：加载合适的Whisper模型
6. **初次转录**：转录所有段落
7. **质量检查**：识别低质量段落
8. **重转录**：改进质量差的段落
9. **断句处理**：添加标点符号
10. **主题识别**：分析内容主题
11. **保存结果**：生成最终文件

## 💡 使用技巧

### 长视频处理
- 系统自动处理，无需手动分割
- 长视频会需要更多时间，请耐心等待
- 可使用 `base` 模型加速处理

### 批量处理
```bash
# 创建批处理文件
for url in "url1" "url2" "url3"; do
    python scripts/smart_transcribe.py "$url"
done
```

### 故障恢复
如果程序中断，可以：
1. 检查任务目录中的进度
2. 重新运行相同命令
3. 系统会自动跳过已完成的段落

## ❗ 注意事项

1. **首次使用**：第一次会下载Whisper模型（需要时间）
2. **网络稳定**：确保下载过程不被中断
3. **磁盘空间**：需要有足够的存储空间
4. **内存要求**：大模型需要大量内存（建议16GB+）
5. **版权问题**：请遵守相关版权法律法规

## 🔍 常见问题

### Q: 下载模型很慢怎么办？
A: 可以手动下载模型或使用较小的模型。

### Q: 转录质量不满意？
A: 系统会自动重录低质量段落，也可以手动使用large-v3模型。

### Q: 程序卡住不动？
A: 可能是在加载模型，请耐心等待。超过10分钟无响应可尝试重启。

### Q: 如何自定义主题？
A: 修改代码中的主题关键词字典，可以添加新的分类。

## 📞 技术支持

遇到问题请提供以下信息：
1. 操作系统版本
2. Python版本
3. 错误信息
4. 使用的URL

---
*Link-Text Skill v3.0.0*